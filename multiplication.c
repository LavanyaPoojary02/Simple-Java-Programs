#include <stdio.h>

int main() {
    int num1, num2, multiplication;

    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    multiplication = num1 * num2;

    printf("Sum = %d", sums);

    return 0;
}

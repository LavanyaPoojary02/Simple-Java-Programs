#include <stdio.h>

int main() {
    int num1, num2, sum;

    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    substraction = num1 - num2;

    printf("Sum = %d", substraction );

    return 0;
}
